<div class="trait-home">
  <div class="catching-phrase">
    <h1 class="main-title">Welcome to DnDummy</h1>
  </div>
  <div class="traits-container">
    <% @traits.each do |t| %>
      <div class="trait">
        <%= link_to t.name, traits_show_path(t) %>
      </div>
    <% end %>
  </div>
      <%= link_to "Back", handbook_path, class: 'back-button'%>
</div>
